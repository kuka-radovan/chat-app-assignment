# Ports
SERVER_PORT=3000
CLIENT_PORT=9000
POSTGRES_PORT=5432

# Production only: host port for the nginx reverse proxy (static client + API/WebSocket)
PROXY_PORT=8080

# Database
POSTGRES_DB=ux-tweak-chat
POSTGRES_USER=uxtweak
POSTGRES_PASSWORD=P@ssw0rd

# Server
NODE_ENV=development


DATABASE_URL=postgresql://uxtweak:P%40ssw0rd@localhost:5432/ux-tweak-chat
DOCKER_DATABASE_URL=postgresql://uxtweak:P%40ssw0rd@postgres:5432/ux-tweak-chat
CORS_ORIGIN=http://localhost:9000 # Must match CLIENT_PORT — http://localhost:<CLIENT_PORT>
